---
// src/components/ui/Hero.astro
import { brand } from "~/lib/brand";
import { srcsetFor } from "~/lib/images";
import { Phone } from "@lucide/astro";

interface Props {
  title: string;
  subtitle?: string;
  ctaText?: string;
  ctaLink?: string;
  isSensitive?: boolean;
  backgroundImage?: string;
}

const { title, subtitle, ctaText = "Get Help Now", ctaLink = "tel:18001234567", isSensitive = false, backgroundImage } = Astro.props;

// Responsive variants (generated by scripts/resize_images.py). Full-bleed
// background sized by the container, so srcset/sizes only — no width/height.
const bg = backgroundImage ? srcsetFor(backgroundImage) : {};
---

<header class="relative overflow-hidden border-t border-white/10 min-h-[60vh] md:min-h-[560px] md:h-[calc(100vh-188px)] flex items-center">
  {/* Background image or solid dark color */}
  {backgroundImage ? (
    <div class="absolute inset-0 z-0">
      <img src={backgroundImage} srcset={bg.srcset} sizes={bg.srcset ? "100vw" : undefined} alt="Background" loading="eager" fetchpriority="high" decoding="async" class="w-full h-full object-cover object-[center_25%]" />
      <div class="absolute inset-0 bg-dark-900/30"></div>
    </div>
  ) : (
    <div class="absolute inset-0 bg-dark z-0"></div>
  )}
  
  {/* Left-to-right scrim: strong behind the text block, light over the photo subject */}
  <div class="absolute inset-0 bg-gradient-to-r from-dark-950/80 via-dark-900/45 to-dark-900/10 z-0"></div>
  <div class="absolute inset-0 bg-[radial-gradient(ellipse_at_center,_var(--tw-gradient-stops))] from-white/5 to-transparent z-0"></div>

  <div class="container-wide relative z-10 pt-10 pb-14 md:pt-12 md:pb-16 text-left">
    <div class="max-w-2xl">
      <h1 class="mb-6 drop-shadow-lg text-4xl md:text-5xl lg:text-6xl">{title}</h1>
      {subtitle && <p class="text-xl md:text-2xl text-white/90 mb-10 drop-shadow-md">{subtitle}</p>}
      <div class="flex flex-col sm:flex-row gap-4 items-start sm:items-center flex-wrap">
        <a href={ctaLink} class="group inline-flex flex-col items-center justify-center bg-primary-600 hover:bg-primary-700 text-white rounded-xl shadow-[0_0_40px_rgba(220,38,38,0.4)] px-8 py-3 transition-transform hover:-translate-y-1 border-2 border-white">
          <span class="text-xs md:text-sm font-bold uppercase tracking-widest text-white/90 mb-1">24/7 Disaster Hotline</span>
          <span class="text-2xl sm:text-3xl md:text-4xl font-black tracking-tight whitespace-nowrap flex items-center gap-3">
            <div class="bg-white text-primary-600 rounded-full p-2 shadow-inner">
              <Phone size={24} strokeWidth={3} />
            </div>
            {brand.phone}
          </span>
        </a>
        <a href="/contact/" class="inline-flex items-center justify-center gap-2 bg-white/10 hover:bg-white/20 text-white font-bold rounded-xl px-6 py-4 transition-all border-2 border-white/40 hover:border-white text-sm tracking-wide whitespace-nowrap">
          Free Estimate &rarr;
        </a>
      </div>
    </div>
  </div>
</header>
